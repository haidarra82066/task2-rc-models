# Python dependencies for the Task 2 RC-model notebooks
# Install with:  pip install -r requirements.txt
numpy
pandas
scipy
plotly
numba
# Optional: only needed to (re)run notebooks interactively
jupyter
